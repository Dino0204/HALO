# 5·18민주화운동 — 화면 구성 기획서 (씬별 스토리보드)

> 이 문서는 `518_hackathon_brief.md`의 기술 명세를 기반으로,
> 각 씬의 시각 요소·역사 내용·UI 레이아웃을 구체적으로 정의한다.
> Claude Code는 이 문서를 씬 단위로 순서대로 구현한다.

---

## 전체 흐름 개요

```
Scene 00  한국 지도 등장 + 배경 설명         (1979.10.26)
Scene 01  12·12 군사 쿠데타 — 서울 점령      (1979.12.12)
Scene 02  전국 대학가 민주화 시위             (1980년 봄)
Scene 03  한국 지도 → 광주 확대 → 전남대 확대 (5.17~5.18 오전)
Scene 04  공수부대 진압 + 최루탄 파티클        (5.18 오전)
Scene 05  택시·버스 차량 시위 — 금남로        (5.20 밤)
Scene 06  광주MBC 방화                        (5.20 21:50)   ★ 신규 + GLB
Scene 07  집단 발포                           (5.21 13:00)
Scene 08  전일빌딩 헬기 사격                  (5.21 오후)    ★ 신규 + GLB
Scene 09  한국 지도 — 광주 봉쇄 (빨간 빗금)   (5.21~5.22)
Scene 10  5.18민주광장 제1차 시민궐기대회     (5.23 13:00)  ★ 신규 + GLB
Scene 11  장갑차 도청 진입                    (5.27 04:00)
Scene 12  병원 — 부상자 통계                  (항쟁 후)
Scene 13  기억 — 국립5.18민주묘지              (현재)        ★ 강화 + GLB
```

scroll 1페이지(100vh) = 씬 1개 기준.
`<ScrollControls pages={14} damping={0.15}>`

★ 표시 4개 씬은 광주문화예술인문스토리플랫폼의 GLB 모델을 활용한다 (CC BY-SA 4.0, 저작자 표시).

---

## 사실 검증 메모

본 기획의 모든 일자·시각은 5·18민주화운동기록관(518archives.go.kr)과 한국어 위키백과 「5·18 광주민주화운동」 문서로 교차 검증했다.

| 사건                  | 일시            | 비고                                  |
| --------------------- | --------------- | ------------------------------------- |
| 광주MBC 방화          | 5.20 21:50      | 언론 통제에 대한 시민 분노             |
| 광주역 발포           | 5.20 23:00      | 시민 4명 사망 (본 기획엔 미포함)       |
| 도청 앞 집단발포      | 5.21 13:00      | 공식 집계 45명 사망 (당일)             |
| 제1차 시민궐기대회    | **5.23 13:00**  | 봉쇄 이후 시민자치기 시작              |
| 제2차 / 3차 / 4차     | 5.24/5.25/5.26  | 도청 앞 광장에서 매일 개최             |
| 도청 진압             | 5.27 04:00      | "광주재진입작전"                       |
| 전일빌딩 245개 탄흔   | 2017 국과수 발표 | 헬기 사격의 공식 실증                  |
| 사상자 통계 165 / 65  | —               | 5·18 유공자 유족회 등 4개 단체 발표    |

> 사상자 통계는 발표 기관별로 수치가 다르다 (광주광역시 2009년 집계: 사망 163명, 부상 3,139명 등). 본 기획에서는 4개 단체 합의 수치를 기준으로 표기하되, 통계가 단일하지 않다는 사실을 본문에 함께 명시한다.

---

## 고정 HUD 요소

씬이 바뀌어도 항상 화면에 유지되는 두 가지 요소.
Three.js 씬 외부, HTML 레이어(`TextOverlay.jsx`)에서 관리한다.

---

### HUD-A: 우측 타임라인 (날짜 진행 표시)

**위치**: 화면 우측 고정, 수직 중앙 정렬
**표시 방식**: 시작일 · 현재일(강조) · 종료일을 수직 선으로 연결

```
5.18        ← 항쟁 시작일, 항상 표시, 작은 크기, opacity 0.5
 ㅣ
 ㅣ         ← 진행 선: 현재 씬 위치에 따라 채워짐 (흰색 → 회색)
 ㅣ
5.21        ← 현재 씬의 날짜, 크고 밝게 강조 (2× 크기, 흰색)
 ㅣ
 ㅣ
 ㅣ
5.27        ← 항쟁 종료일, 항상 표시, 작은 크기, opacity 0.5
```

**씬별 날짜 데이터**

| 씬  | HUD 표시 날짜       | 강조 여부                    |
| --- | ------------------- | ---------------------------- |
| 00  | — (HUD 비표시)      | —                            |
| 01  | 1979.12.12          | — (항쟁 전, 별도 스타일)     |
| 02  | 1980년 봄           | —                            |
| 03  | 5.18                | 강조                         |
| 04  | 5.18                | 강조                         |
| 05  | 5.20                | 강조                         |
| 06  | 5.20                | 강조 (광주MBC 방화)          |
| 07  | 5.21                | 강조 (집단 발포)             |
| 08  | 5.21                | 강조 (전일빌딩 헬기 사격)    |
| 09  | 5.21                | 강조 (봉쇄)                  |
| 10  | 5.23                | 강조 (시민궐기대회)          |
| 11  | 5.27                | 강조 (종료일과 동일, 붉은색) |
| 12  | 5.27                | 강조                         |
| 13  | — (HUD 페이드 아웃) | —                            |

> 씬 01~02는 항쟁 이전 배경이므로 타임라인 HUD를 표시하지 않거나
> 별도의 "배경" 레이블로 대체한다.
> 씬 03부터 타임라인이 fade in되어 등장한다.

**진행 선 채움 계산**

```javascript
// 5.18 ~ 5.27 = 9일, 각 날짜의 진행도(0~1)
const DATE_PROGRESS = {
  5.18: 0 / 9, // 0.000
  5.19: 1 / 9, // 0.111
  '5.20': 2 / 9, // 0.222
  5.21: 3 / 9, // 0.333
  5.22: 4 / 9,
  5.23: 5 / 9,
  5.24: 6 / 9,
  5.25: 7 / 9,
  5.26: 8 / 9,
  5.27: 9 / 9, // 1.000
}
// 진행 선의 채움 높이 = DATE_PROGRESS[currentDate] × 전체 선 높이
```

**스타일 명세**

```css
/* HUD 컨테이너 */
.hud-timeline {
  position: fixed;
  right: 2.5rem;
  top: 50%;
  transform: translateY(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0;
  z-index: 20;
  pointer-events: none;
}

/* 시작일 / 종료일 */
.hud-date-edge {
  font-family: monospace;
  font-size: 0.75rem;
  color: #ffffff;
  opacity: 0.45;
  letter-spacing: 0.1em;
}

/* 현재 강조 날짜 */
.hud-date-current {
  font-family: monospace;
  font-size: 1.1rem;
  color: #ffffff;
  opacity: 1;
  letter-spacing: 0.12em;
  font-weight: 700;
  text-shadow: 0 0 12px rgba(255, 255, 255, 0.4);
  transition: all 0.6s ease;
}

/* 씬 08 (5.27 최후) 일 때만 붉은색으로 */
.hud-date-current.final {
  color: #ff6666;
  text-shadow: 0 0 12px rgba(255, 80, 80, 0.5);
}

/* 진행 선 — 배경 (회색) */
.hud-line-track {
  width: 1px;
  height: 120px;
  background: rgba(255, 255, 255, 0.15);
  position: relative;
}

/* 진행 선 — 채움 (흰색) */
.hud-line-fill {
  position: absolute;
  top: 0;
  width: 100%;
  background: rgba(255, 255, 255, 0.7);
  transition: height 0.8s ease;
}
```

**React 컴포넌트 구조**

```jsx
// ui/HUDTimeline.jsx
export default function HUDTimeline({ currentScene }) {
  const sceneData = SCENE_HUD_DATA[currentScene]
  if (!sceneData || !sceneData.visible) return null

  const progress = DATE_PROGRESS[sceneData.date] ?? 0
  const isFinal = sceneData.date === '5.27'

  return (
    <div className="hud-timeline">
      {/* 시작일 */}
      <span className="hud-date-edge">5.18</span>

      {/* 위쪽 선 — 시작일 ~ 현재 */}
      <div className="hud-line-track" style={{ height: '100px' }}>
        <div className="hud-line-fill" style={{ height: `${progress * 100}%` }} />
      </div>

      {/* 현재 날짜 강조 */}
      <span className={`hud-date-current ${isFinal ? 'final' : ''}`}>{sceneData.date}</span>

      {/* 아래쪽 선 — 현재 ~ 종료일 */}
      <div className="hud-line-track" style={{ height: '100px' }}>
        <div
          className="hud-line-fill"
          style={{ height: `${(1 - progress) * 100}%`, top: 'auto', bottom: 0 }}
        />
      </div>

      {/* 종료일 */}
      <span className="hud-date-edge">5.27</span>
    </div>
  )
}
```

---

### HUD-B: 좌측 상단 위치 표시

**위치**: 화면 좌측 상단 고정, `top: 2rem, left: 2rem`
**표시 형식**:

```
대한민국 / 광주
         전남대학교 정문
```

- 상위 지역과 하위 장소를 `/` 또는 줄바꿈으로 계층 표현
- 씬 전환 시 이전 텍스트 fade out → 새 텍스트 fade in (0.4s)

**씬별 위치 데이터**

| 씬  | 상위 지역 | 하위 장소       |
| --- | --------- | --------------- |
| 00  | 대한민국  | —               |
| 01  | 대한민국  | 서울            |
| 02  | 대한민국  | 전국 주요 도시  |
| 03  | 광주      | 전남대학교      |
| 04  | 광주      | 전남대학교 정문 |
| 05  | 광주      | 금남로          |
| 06  | 광주      | 광주MBC         |
| 07  | 광주      | 금남로          |
| 08  | 광주      | 전일빌딩        |
| 09  | 광주      | — (봉쇄 중)     |
| 10  | 광주      | 도청 앞 광장    |
| 11  | 광주      | 전남도청        |
| 12  | 광주      | —               |
| 13  | —         | — (페이드 아웃) |

**스타일 명세**

```css
.hud-location {
  position: fixed;
  top: 2rem;
  left: 2rem;
  z-index: 20;
  pointer-events: none;
}

.hud-location-region {
  font-family: monospace;
  font-size: 0.7rem;
  color: #ffffff;
  opacity: 0.5;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  margin-bottom: 0.2rem;
}

/* 구분자 슬래시 + 하위 장소 */
.hud-location-place {
  font-family: 'Noto Serif KR', serif;
  font-size: 0.95rem;
  color: #e8e0d0;
  opacity: 0.85;
  letter-spacing: 0.08em;
}
```

**React 컴포넌트 구조**

```jsx
// ui/HUDLocation.jsx
export default function HUDLocation({ currentScene }) {
  const { region, place } = SCENE_HUD_DATA[currentScene]?.location ?? {}

  return (
    <div className="hud-location">
      {region && <div className="hud-location-region">{region}</div>}
      {place && <div className="hud-location-place">{place}</div>}
    </div>
  )
}
```

---

### HUD 통합 데이터 테이블 (`data/hudData.js`)

```javascript
export const SCENE_HUD_DATA = {
  0: { visible: false, date: null, location: { region: '대한민국', place: null } },
  1: { visible: false, date: '1979.12.12', location: { region: '대한민국', place: '서울' } },
  2: { visible: false, date: '1980년 봄', location: { region: '대한민국', place: '전국 주요 도시' } },
  3: { visible: true,  date: '5.18', location: { region: '광주', place: '전남대학교' } },
  4: { visible: true,  date: '5.18', location: { region: '광주', place: '전남대학교 정문' } },
  5: { visible: true,  date: '5.20', location: { region: '광주', place: '금남로' } },
  6: { visible: true,  date: '5.20', location: { region: '광주', place: '광주MBC' } },
  7: { visible: true,  date: '5.21', location: { region: '광주', place: '금남로' } },
  8: { visible: true,  date: '5.21', location: { region: '광주', place: '전일빌딩' } },
  9: { visible: true,  date: '5.21', location: { region: '광주', place: null } },
  10:{ visible: true,  date: '5.23', location: { region: '광주', place: '도청 앞 광장' } },
  11:{ visible: true,  date: '5.27', location: { region: '광주', place: '전남도청' } },
  12:{ visible: true,  date: '5.27', location: { region: '광주', place: null } },
  13:{ visible: false, date: null,   location: { region: null, place: null } },
}

export const DATE_PROGRESS = {
  5.18: 0 / 9,
  5.19: 1 / 9,
  '5.20': 2 / 9,
  5.21: 3 / 9,
  5.22: 4 / 9,
  5.23: 5 / 9,
  5.24: 6 / 9,
  5.25: 7 / 9,
  5.26: 8 / 9,
  5.27: 9 / 9,
}
```

---

### TextOverlay.jsx 통합 구조 (HUD 포함)

```jsx
// ui/TextOverlay.jsx
import HUDTimeline from './HUDTimeline'
import HUDLocation from './HUDLocation'
import { useCurrentScene } from '../scene/Experience'

export default function TextOverlay() {
  const currentScene = useCurrentScene()

  return (
    <div style={{ position: 'fixed', inset: 0, pointerEvents: 'none', zIndex: 10 }}>
      {/* HUD-A: 우측 타임라인 */}
      <HUDTimeline currentScene={currentScene} />

      {/* HUD-B: 좌측 상단 위치 */}
      <HUDLocation currentScene={currentScene} />

      {/* 씬별 중앙 텍스트 — 별도 컴포넌트에서 관리 */}
      <SceneText currentScene={currentScene} />
    </div>
  )
}
```

> `useCurrentScene`은 `Experience.jsx`에서 export하되,
> Three.js 컨텍스트 외부(`TextOverlay`)에서 사용하려면
> `zustand` 또는 React `Context`로 씬 상태를 공유해야 한다.
> 가장 단순한 구현: `zustand`의 `useSceneStore`를 만들어
> `CameraRig.jsx`의 `useFrame` 안에서 씬 번호를 set하고
> `TextOverlay.jsx`에서 구독한다.

```javascript
// store/sceneStore.js
import { create } from 'zustand'

export const useSceneStore = create((set) => ({
  currentScene: 0,
  setScene: (scene) => set({ currentScene: scene }),
}))
```

---

## Scene 00 — 한국 지도 등장 + 배경 설명

**scroll 0% ~ 7%**

### 시각 요소

- **Three.js 오브젝트**
  - 한국 지도 SVG path를 `THREE.ShapeGeometry`로 변환한 평면 메시
  - 색상: `#2a2a2a` (어두운 회색), 테두리 라인 `#4a4a4a`
  - 초기 카메라: 한반도 전체가 화면에 들어오는 탑뷰 (`position: 0, 80, 0`)
  - 지도 주변은 완전한 어둠 (`scene.background = #0a0a0a`)

- **셰이더**: 흑백 100%, grain 0.10

### 텍스트 오버레이 (순서대로 등장)

```
[중앙 상단]
"대한민국, 1979년"
— SplitText, 글자 단위 stagger 0.04s, 등장 후 1.5s 유지

[중앙]
"18년간의 독재가 끝났다."
— fade in, 0.8s delay

[중앙 하단, 작은 크기]
"1979년 10월 26일, 박정희 대통령 피격 사망"
— monospace, opacity 0.6, 0.3s delay
```

### 데이터 (`timeline.json` 항목)

```json
{
  "scene": 0,
  "date": "1979-10-26",
  "title": "독재의 종말",
  "text": "18년 장기 독재 박정희 대통령이 측근 김재규에게 암살된다. 민주화에 대한 기대가 전국을 감쌌다."
}
```

---

## Scene 01 — 12·12 군사 쿠데타: 서울 점령

**scroll 7% ~ 14%**

### 시각 요소

- **지도 위 애니메이션**
  - 서울 위치에 빨간 점(`THREE.SphereGeometry`, radius 0.3) 등장
  - 서울을 중심으로 동심원 파문이 퍼져나감
    - `THREE.RingGeometry`를 `useFrame`에서 scale 1→4, opacity 1→0으로 반복
    - 파문 3겹, 0.3s stagger
  - 파문과 함께 서울 점 색상이 `#ff3333`으로 강해짐

- **지도 전체**
  - 서울을 제외한 나머지 지도 색상이 `#1a1a1a`로 더 어두워짐
  - 어둠이 서울에서 바깥쪽으로 퍼지는 효과 (거리 기반 uniform)

- **셰이더**: 흑백 유지, grain 0.12 (긴장감)

### 텍스트 오버레이

```
[좌측 상단, 날짜 태그]
"1979.12.12"
— monospace, #ff6666, 즉시 등장

[중앙]
"전두환, 군사 반란을 일으키다"
— SplitText, 단어 단위 stagger

[중앙 하단]
"군부가 서울을 장악했다.
 국회는 봉쇄되고, 민주주의는 멈췄다."
— Noto Serif KR, 2줄, fade in 0.6s delay
```

### Three.js 구현 힌트

```jsx
// 서울 좌표 → Three.js 로컬 좌표 (한국 지도 기준)
// 지도 중심을 원점(0,0)으로 정규화한 뒤 적용
const SEOUL_POS = new THREE.Vector3(-0.8, 2.1, 0) // 지도 메시 좌표계 기준

// 파문 링
function PulseRing({ position, delay }) {
  const ref = useRef()
  useFrame(({ clock }) => {
    const t = (clock.elapsedTime - delay) % 2.0
    ref.current.scale.setScalar(1 + t * 1.5)
    ref.current.material.opacity = Math.max(0, 1 - t / 2.0)
  })
  return (
    <mesh ref={ref} position={position} rotation={[-Math.PI / 2, 0, 0]}>
      <ringGeometry args={[0.3, 0.35, 32]} />
      <meshBasicMaterial color="#ff3333" transparent opacity={1} />
    </mesh>
  )
}
```

---

## Scene 02 — 전국 대학가 민주화 시위

**scroll 14% ~ 21%**

### 시각 요소

- **지도 위 마커**
  - 광역시 6곳(서울, 부산, 대구, 인천, 광주, 대전) 위치에
    태극기 아이콘 또는 흰색 원형 마커 순서대로 등장
  - 등장 순서: 0.2s stagger, scale 0→1 bounce ease
  - 마커 색상: 흰색 `#ffffff`, 크기 반경 0.25

- **마커 레이블**
  - `@react-three/drei`의 `<Html>` 컴포넌트로 각 도시명 표시
  - 폰트: monospace, 흰색, 8px

- **지도 분위기**
  - 마커들이 등장하면서 지도 전체가 미세하게 밝아짐 (`#2a2a2a` → `#333333`)

- **셰이더**: 흑백, grain 0.08 (이전 씬보다 약간 완화)

### 텍스트 오버레이

```
[좌측 상단]
"1980년 봄"

[중앙]
"전국 대학가에서 민주화의 목소리가 터져 나왔다."

[하단, 도시들이 등장하면서 함께]
"서울 · 부산 · 대구 · 광주 · 대전 · 인천"
— 각 도시명이 마커 등장에 맞춰 stagger
```

### 광역시 좌표 (한국 지도 정규화 기준)

```javascript
// 지도 SVG 원본 좌표를 Three.js 좌표계로 변환한 값
// 실제 지도 메시 생성 시 같은 변환 행렬 적용 필요
export const CITIES = [
  { name: '서울', pos: new THREE.Vector3(-0.8, 2.1, 0.01) },
  { name: '인천', pos: new THREE.Vector3(-1.1, 2.0, 0.01) },
  { name: '대전', pos: new THREE.Vector3(-0.6, 1.2, 0.01) },
  { name: '대구', pos: new THREE.Vector3(0.3, 0.8, 0.01) },
  { name: '부산', pos: new THREE.Vector3(0.6, -0.1, 0.01) },
  { name: '광주', pos: new THREE.Vector3(-0.9, -0.2, 0.01) },
]
```

---

## Scene 03 — 한국 지도 → 광주 확대 → 전남대 확대

**scroll 21% ~ 29%**

### 시각 요소 — 3단계 카메라 줌

**3-1. 지도에서 광주로 줌인**

- 카메라가 광주 마커를 향해 부드럽게 이동
- `camera.position`: `(0, 80, 0)` → `(광주X, 15, 광주Z)`
- 다른 도시 마커들이 fade out
- 광주 마커만 남아 강조 (색상 `#ff6666`으로 변환)

**3-2. 광주 시내 추상 건물 격자 등장**

- 지도 메시 위에 광주 시내 추상 건물들이 돌출(`ExtrudeGeometry`)
- 색상: `#222222`, 높이 랜덤 0.1~0.5

**3-3. 전남대 위치 확대**

- 전남대 위치에 별도 마커 (흰 사각형 `#ffffff`)
- 텍스트 레이블: "전남대학교"
- 카메라가 추가로 줌인: `(광주X, 15, 광주Z)` → `(전남대X, 4, 전남대Z)`

### 텍스트 오버레이

```
[3-1 구간]
"그 중심에 광주가 있었다."

[3-2 구간]
"1980년 5월 17일
 신군부는 비상계엄을 전국으로 확대했다."

[3-3 구간]
"5월 18일 오전
 전남대학교 정문 앞"
— 확대와 함께 fade in
```

---

## Scene 04 — 공수부대 진압 + 최루탄 파티클

**scroll 29% ~ 36%**

### 시각 요소

**배경**

- 전남대 정문 앞 거리 — 추상화된 건물 실루엣 (1인칭에 가까운 시점)
- 카메라 높이 `y: 1.8` (사람 눈높이)

**최루탄 파티클 시스템**

- `THREE.Points` + 커스텀 셰이더
- 파티클 수: 800개
- 초기 위치: 화면 중앙 전방 랜덤 분포
- 운동: Y축 위로 서서히 상승 + 좌우 미세 확산 (바람 효과)
- 색상: 흰색 → 연회색 (`#ffffff` → `#888888`), 시간에 따라 변환
- 파티클 크기: 가까울수록 크게 (포인트 스프라이트 거리 감쇠)

**셰이더 변화**

- grain 강도 0.15로 증가 (혼란, 공포)
- vignette 강도 0.6으로 증가 (터널 시야, 공포 연출)
- 화면 전체에 미세한 빨간색 tint `mix(blended, vec3(0.3, 0.0, 0.0), 0.08)`

### 텍스트 오버레이

```
[좌측 상단]
"1980.05.18"
— #ff6666

[중앙 하단]
"계엄군이 학생들을 향해 진압봉을 휘둘렀다.
 지나가던 시민도 예외가 아니었다."
— Noto Serif KR

[하단 작게]
"전남대학교 정문 앞 — 오전 10시"
— monospace, opacity 0.5
```

### 최루탄 파티클 구현 힌트

```jsx
// Particles.jsx
const PARTICLE_COUNT = 800

export default function TearGasParticles({ active }) {
  const points = useRef()
  const positions = useMemo(() => {
    const arr = new Float32Array(PARTICLE_COUNT * 3)
    for (let i = 0; i < PARTICLE_COUNT; i++) {
      arr[i * 3] = (Math.random() - 0.5) * 20 // x
      arr[i * 3 + 1] = Math.random() * 5 // y
      arr[i * 3 + 2] = (Math.random() - 0.5) * 10 // z
    }
    return arr
  }, [])

  useFrame(({ clock }) => {
    const t = clock.elapsedTime
    const pos = points.current.geometry.attributes.position.array
    for (let i = 0; i < PARTICLE_COUNT; i++) {
      pos[i * 3 + 1] += 0.005 // 상승
      if (pos[i * 3 + 1] > 12) pos[i * 3 + 1] = 0 // 리셋
    }
    points.current.geometry.attributes.position.needsUpdate = true
  })

  return (
    <points ref={points}>
      <bufferGeometry>
        <bufferAttribute
          attach="attributes-position"
          array={positions}
          count={PARTICLE_COUNT}
          itemSize={3}
        />
      </bufferGeometry>
      <pointsMaterial
        size={0.08}
        color="#cccccc"
        transparent
        opacity={0.6}
        sizeAttenuation
        depthWrite={false}
      />
    </points>
  )
}
```

---

## Scene 05 — 택시·버스 차량 시위: 금남로

**scroll 36% ~ 43%**

### 시각 요소

**카메라**

- 금남로 정면 탑뷰에서 약간 기울인 시점 (45도 각도)
- `camera.position: (0, 20, 20)`, lookAt: `(0, 0, 0)`

**도로**

- 금남로를 표현하는 긴 회색 평면 `PlaneGeometry` (폭 6, 길이 60)
- 도로 중앙선: `LineGeometry` 흰색 점선

**차량 행렬**

- 택시: 노란색 `BoxGeometry` (1.2 × 0.6 × 2.4), 30대
- 버스: 파란색 `BoxGeometry` (2.0 × 1.0 × 5.0), 10대
- 차량들이 도로 위에 빼곡하게 배치 (z축으로 -3 간격)
- 전조등 효과: 각 차량 앞에 `PointLight` 흰색, intensity 0.5, distance 3

**차량 등장 애니메이션**

- 차량들이 화면 아래에서 위로 천천히 진입 (z축 이동)
- `useFrame`으로 `position.z -= 0.02`
- 화면 끝 도달 시 반대편으로 재배치 (루프)

**셰이더**

- 이 씬만 grain 0.10으로 완화
- vignette 0.3 (이전 씬보다 개방감)
- 흑백 85% (약간의 노란색이 비쳐 보이는 느낌)

### 텍스트 오버레이

```
[좌측 상단]
"1980.05.20 밤"

[중앙]
"수백 대의 택시와 버스가
 금남로를 가득 메웠다."

[중앙 하단]
"운전기사들이 자발적으로 나섰다.
 전조등이 밤을 밝혔다."
— Noto Serif KR, line-height 1.9
```

---

## Scene 06 — 광주MBC 방화

**scroll 43% ~ 50%**

### 시각 요소

**GLB 모델**: `옛_광주MBC` → `public/models/gwangju-mbc.glb`

- `useGLTF`로 로드, scale ≈ 1, 카메라 정면 측면 3/4 뷰
- 카메라: `position: (8, 5, 14)`, lookAt: `(0, 3, 0)`

**야간 분위기**

- `ambientLight intensity 0.05`
- 배경 `#0a0a14` (5월 20일 늦은 밤)

**화염 표현**

- 1층 창문 위치에 오렌지 `PointLight #ff5500` intensity 0→4 sin 변조 (불꽃 깜빡임 표현)
- `SmokeParticles`: 씬 04 `TearGasParticles`를 재사용하되 색상 `#332211`(검은 연기), 상승 속도 2배, 파티클 수 1500개로 증가
- 건물 위로 검은 연기 기둥이 화면 상단까지 솟음

**군중 표현**

- 건물 앞 도로에 어두운 인영 BoxGeometry 30개 (`#1a1a1a`, 높이 1.8) — 분노한 시민들의 실루엣

**셰이더**: 흑백 100%, grain 0.15, vignette 0.55, 빨간 tint 0.10

### 텍스트 오버레이

```
[좌측 상단, 빨간색]
"1980.05.20 밤 9시 50분"
— monospace, #ff6666

[중앙]
"광주MBC가 불탔다."
— SplitText 단어 stagger

[중앙 하단]
"방송은 시민이 죽어가는 도시를 보도하지 않았다.
 분노한 시민들이 방송국에 불을 질렀다."
— Noto Serif KR, 2줄, line-height 1.8
```

### 역사적 맥락

5월 20일 밤, 광주MBC를 비롯한 방송국은 광주 시민이 죽어가는 현장을 보도하지 않았다. 보안사 통제 하에 시위대를 "불순분자·폭도"로만 보도한 것에 격분한 시위대가 광주MBC 사옥에 불을 질렀다 (21:50). 같은 날 23시에는 광주역에서 계엄군의 발포로 시민 4명이 사망했다.

---

## Scene 07 — 집단 발포

**scroll 50% ~ 57%**

### 시각 요소

**카메라**: 1인칭, 금남로 한가운데, 높이 `y: 1.8`

**발포 효과**

- 화면 전방 여러 지점에서 섬광 효과
  - `PointLight` 흰색 intensity 0→10→0, duration 0.05s, 불규칙 간격
  - `THREE.SphereGeometry` 작은 구 (총구 화염) scale 0→0.3→0 즉시
- 총성 대신 섬광만으로 표현 (오디오 미구현)

**연기 파티클**

- 이전 씬 최루탄 파티클을 재사용하되
  색상을 `#444444` (짙은 회색)으로 변경
  양을 1500개로 증가
  확산 범위 더 넓게

**셰이더 — 이 씬 최대 강도**

- grain 0.18 (공포, 혼란 최대)
- vignette 0.7
- 빨간 tint 0.15
- 섬광 순간에만 화면 전체 flash: uniform `uFlash 0→1→0` 0.05s

### 텍스트 오버레이

```
[좌측 상단, 빨간색]
"1980.05.21 오후 1시"

[중앙, 천천히 fade in]
"계엄군이 시민을 향해 총을 쐈다."

[하단, 시간 두고 등장]
"그날 금남로에서
 수십 명이 쓰러졌다."

[하단 우측, 작게]
"— 5·18기념재단 공식 기록"
— monospace, opacity 0.4
```

---

## Scene 08 — 전일빌딩 헬기 사격

**scroll 57% ~ 64%**

### 시각 요소

**GLB 모델**: `전일빌딩245` → `public/models/jeonil-building.glb`

- 빌딩 정면을 화면 중앙에 배치, scale ≈ 1.2
- 카메라: `position: (0, 4, 18)`, lookAt: `(0, 12, 0)` — 빌딩을 올려다보는 시점

**헬기 표현**

- 상공에 단순화된 헬기 실루엣 `BoxGeometry` (3 × 0.8 × 4, `#1a1a1a`)
- 빌딩 위를 좌→우로 천천히 가로지름 (씬 진행도에 따라 x 이동)
- 로터 회전: 헬기 위 평평한 `CylinderGeometry` 디스크 useFrame `rotation.y += 0.5`
- 헬기 소음 표현: 카메라 미세 진동 `camera.position.x += sin(t*30)*0.02`

**탄흔 마커**

- 빌딩 GLB 외벽 좌표 245개 위치에 빨간 점 `SphereGeometry` 직경 0.05 (또는 `InstancedMesh` 사용)
- 씬 진행도 0.3 이후 점차 fade in, 마지막 0.2 구간에서 모두 표시
- 마커 색상: `#ff3333`, 약한 발광

**셰이더**

- 흑백 100%, grain 0.18, vignette 0.7, 빨간 tint 0.15
- 헬기 통과 순간 0.3초 flash `uFlash 0→1→0`

### 텍스트 오버레이

```
[좌측 상단, 빨간색]
"1980.05.21 오후"
— monospace, #ff6666

[중앙]
"하늘에서도 총탄이 쏟아졌다."
— SplitText 단어 stagger

[하단 중앙]
"전일빌딩 외벽에서 245개의 헬기 사격 탄흔이 발견됐다."
— Noto Serif KR

[하단 우측, 작게]
"— 국립과학수사연구원 공식 감정 (2017)"
— monospace, opacity 0.4
```

### 역사적 맥락

전일빌딩(금남로)은 5월 21일 도청 앞 집단발포(13시)와 같은 시간대에 계엄군의 표적이 되었다. 외벽에 남은 245개의 탄흔은 2017년 국립과학수사연구원의 공식 감정으로 헬기 사격 흔적임이 실증되었다 — 사건이 일어난 지 37년이 지난 뒤였다. 본 어셋의 이름 `전일빌딩245`도 이 탄흔 수에서 유래한다.

---

## Scene 09 — 한국 지도: 광주 봉쇄

**scroll 64% ~ 71%**

### 시각 요소

**카메라**: 다시 한반도 탑뷰로 복귀

- transition: Scene 06의 1인칭 → 탑뷰로 빠르게 상승
- `(전남대X, 1.8, 전남대Z)` → `(0, 80, 0)`, duration: 이 씬 전환 구간

**지도 위 광주**

- 광주 영역에 빨간 빗금 패턴 오버레이
  - `THREE.PlaneGeometry` + GLSL 빗금 패턴 셰이더 (fragCoord 기반 stripe)
  - 스트라이프 각도: 45도, 간격: 4px, 색상: `#cc0000` opacity 0.6
  - 빗금이 광주 외곽 윤곽을 따라 마스킹 (stencil 또는 clip path)

**봉쇄 라인**

- 광주 주변을 두르는 붉은 테두리 라인
  - `THREE.Line` + `LineBasicMaterial` `#ff0000`
  - 선이 광주를 점점 조여드는 애니메이션 (drawRange 0→full)

**나머지 지도**

- 광주 외 지역이 더 어두워짐 `#111111`
- 광주만 빗금으로 강조됨

**셰이더**: 흑백 복귀 100%, grain 0.12

### 텍스트 오버레이

```
[좌측 상단]
"1980.05.21 이후"

[중앙]
"계엄군이 광주를 완전히 차단했다."

[하단]
"전화가 끊겼다.
 도로가 막혔다.
 언론은 침묵했다."
— 3줄 순서대로 stagger 0.4s 간격

[우측 하단, 작게]
"외부 세계는 광주에서 무슨 일이 일어나는지 알지 못했다."
— monospace, opacity 0.5
```

### 빗금 셰이더 패턴

```glsl
// 광주 오버레이 전용 셰이더
void mainImage(const in vec4 inputColor, const in vec2 uv, out vec4 outputColor) {
  // 45도 빗금
  float stripe = mod((uv.x + uv.y) * 40.0, 1.0);
  float mask = step(0.5, stripe);

  vec4 overlay = vec4(0.8, 0.0, 0.0, mask * 0.55);
  outputColor = mix(inputColor, overlay, overlay.a);
}
```

---

## Scene 10 — 5.18민주광장 제1차 시민궐기대회

**scroll 71% ~ 79%**

### 시각 요소

**GLB 모델**: `5.18민주광장_분수대` → `public/models/gwangju-fountain.glb`

- 분수대를 광장 중앙에 배치, scale 1
- 옵션: `옛_전남도청` GLB (씬 11에서 사용)도 동시 로드하여 광장 배경으로 함께 배치 — `useGLTF` 자동 캐시로 중복 로드 없음

**카메라**

- `position: (0, 12, 22)`, lookAt: `(0, 1, 0)` — 광장 전체를 내려다보는 부감 시점

**광장 평면**

- `PlaneGeometry` 40 × 40, `#1a1a1a`

**시민 표현**

- `InstancedMesh`로 시민 박스 (0.4 × 1.8 × 0.4, `#cccccc`) 1500개
- 분수대 주변으로 동심원 분포 (랜덤 jitter 추가)
- 시간에 따라 미세하게 좌우 흔들리는 `useFrame` (군중감 표현)

**햇빛 (5.23 오후 1시)**

- `DirectionalLight` intensity 1.0, 색 `#fff6e0` (따뜻한 햇빛)
- 봉쇄 직후 시민자치가 작동한 짧은 시간의 평온함을 표현

**셰이더**: 흑백 95%, grain 0.10, vignette 0.4 (해방 광주의 일시적 평온감)

### 텍스트 오버레이

```
[좌측 상단]
"1980.05.23 오후 1시"
— monospace, 흰색

[중앙]
"수십만 명이 도청 앞 광장에 모였다."

[중앙 하단]
"제1차 민주수호 범시민궐기대회"
— Noto Serif KR, 1.2rem

[하단 우측, 작게]
"외부 세계와 단절된 광주는 스스로의 질서를 만들어 가고 있었다."
— monospace, opacity 0.5
```

### 역사적 맥락

5월 21일 발포 이후 광주는 봉쇄되었으나, 시민들은 흩어지지 않고 도청 앞 광장에 매일 모여 궐기대회를 열었다. 제1차는 5월 23일 13시에 시작되어, 5월 24일·25일·26일 연속으로 이어졌다. 이 5일간(5.23~5.27 새벽)이 시민자치가 작동한 "해방 광주" 시기였다.

---

## Scene 11 — 장갑차 도청 진입

**scroll 79% ~ 86%**

### 시각 요소

**카메라**: 전남도청 정면, 약간 측면 (3/4 뷰)

- `position: (10, 5, 20)`, lookAt: `(0, 2, 0)`

**도청 건물**

- 추상화된 직사각형 건물 `BoxGeometry` (20 × 8 × 12)
- 색상: `#1a1a1a`, 정면에 창문 텍스처 (작은 흰색 rect들)

**장갑차**

- `BoxGeometry`로 단순화한 장갑차 형태 (4 × 2 × 6)
- 색상: `#1a3a1a` (군용 녹색)
- 도청 정면 도로를 따라 천천히 전진 (`position.z -= 0.03`)
- 장갑차 3대, z축으로 간격 두고 행렬

**새벽 분위기**

- `ambientLight intensity: 0.1` (극도로 어두운)
- 장갑차 헤드라이트: `SpotLight` 흰색, 전방 조사
- 하늘 (배경): `#0a0a14` (새벽 4시 어둠)

**셰이더**

- 흑백 90%, grain 0.14, vignette 0.6

### 텍스트 오버레이

```
[좌측 상단, 빨간색]
"1980.05.27 새벽 4시"

[상단 중앙, 먼저 등장]
"지금 계엄군이 쳐들어오고 있습니다.
 시민 여러분, 우리를 잊지 말아 주십시오."
— Noto Serif KR, 인용 형식, 이탤릭
— 이 씬에서 가장 중요한 텍스트
— font-size 1.4rem, 천천히 fade in 1.5s

[하단]
"도청에 남은 시민군은 끝까지 싸웠다."
— 0.8s delay 후 등장
```

---

## Scene 12 — 병원: 부상자 통계

**scroll 86% ~ 93%**

### 시각 요소

**카메라**: 정면 고정, 약간 위에서 내려다보는 시점

- `position: (0, 6, 15)`, lookAt: `(0, 2, 0)`

**병원 시각화**

- 흰색 십자가 `PlaneGeometry` + 흰색 텍스처, 중앙 배치
- 십자가 아래로 통계 수치들이 순서대로 카운트 업 애니메이션

**통계 카드 — Three.js `<Html>` 컴포넌트**

- 카드 4개, 2×2 그리드 배치
- 각 카드: 어두운 배경 `rgba(0,0,0,0.8)`, 테두리 `#555`

```
┌─────────────────┐  ┌─────────────────┐
│   사망 인정      │  │   행방불명       │
│   165명          │  │   65명           │
└─────────────────┘  └─────────────────┘
┌─────────────────┐  ┌─────────────────┐
│   부상           │  │   구속·구금      │
│   3,028명        │  │   1,589명        │
└─────────────────┘  └─────────────────┘
```

**카운트 업 애니메이션**

- 숫자가 0에서 실제 수치로 2초간 카운트 업
- GSAP `gsap.to(obj, { value: 165, duration: 2, ease: "power2.out" })`

**셰이더**

- 이 씬에서 흑백 50% → 약간 컬러 허용 시작
- grain 0.06으로 감소 (다소 정적인 분위기)

### 텍스트 오버레이

```
[상단]
"9일간의 기록"

[통계 카드 아래]
"이것은 공식적으로 인정된 숫자다.
 발표 기관에 따라 수치는 조금씩 다르며,
 모든 피해가 완전히 밝혀지지 않았다."
— Noto Serif KR, opacity 0.7, 3줄

[하단 작게]
"출처: 5·18 유공자 유족회 등 4개 단체 발표 (사망 165 · 행불 65)"
— monospace, opacity 0.4

[보조 참고, 더 작게]
"광주광역시 2009년 집계는 사망 163명, 부상 3,139명으로 다르게 표기됨."
— monospace, opacity 0.3
```

---

## Scene 13 — 기억: 국립5.18민주묘지 + 아카이브

**scroll 93% ~ 100%**

이 씬은 두 구간으로 나뉜다.
- **13-A** (씬 진행도 0~0.5): 한반도 컬러 복귀 + 광주 빛 번짐 + 아카이브 카드 4개
- **13-B** (씬 진행도 0.5~1): 카메라가 한반도에서 국립5.18민주묘지로 줌인 → 묘지 모델 클로즈업

### 13-B 추가 시각 요소

**GLB 모델**: `국립5.18민주묘지` → `public/models/may18-cemetery.glb`

- 모델 중심을 원점에 배치
- 카메라가 GSAP 트윈으로 `(0, 80, 0)` (한반도 탑뷰) → `(15, 8, 25)` (묘지 3/4 뷰)로 전환
- `DirectionalLight` 따뜻한 색 `#ffd9a0` intensity 1.2
- 추모 입자: 묘비 위로 흰 빛 입자 50개 (`PointMaterial` 작은 흰 점, 위쪽으로 천천히 상승)
- 옅은 안개 효과 옵션: `Fog`로 원경 흐림

**셰이더**: 컬러 100%, grain 0.03, vignette 0.1 — 가장 평온한 톤

### 시각 요소

**카메라**: 다시 한반도 탑뷰, 하지만 이번엔 카메라가 느리게 후퇴하며 전체를 봄

- `position: (0, 40, 0)` → `(0, 120, 0)` 천천히

**지도 변화**

- 흑백에서 컬러로 완전 전환 (Scene 07의 빗금 사라짐)
- 광주 위치에 따뜻한 빛의 점이 천천히 퍼짐
  - `PointLight` 오렌지 `#ff8c00`, intensity 0→3, distance 15
  - 광주 중심에서 빛이 한반도 전체로 번지는 느낌

**아카이브 자료 카드 (`<Html>` 컴포넌트)**

- 지도 주변 여백에 자료 카드 4~6개 배치
- 각 카드는 scroll에 따라 순서대로 fade in

| 카드   | 내용                                                           |
| ------ | -------------------------------------------------------------- |
| 카드 1 | "2002년, 5월 18일이 국가기념일로 지정됐다."                    |
| 카드 2 | "2011년, 5·18 관련 기록물이 유네스코 세계기록유산에 등재됐다." |
| 카드 3 | "현재도 암매장 희생자 유해 발굴이 진행 중이다."                |
| 카드 4 | "5·18은 이후 필리핀·태국 민주화 운동에 직접적인 영향을 줬다."  |

**셰이더**: 컬러 100%, grain 0.03, vignette 최소화 0.1

### 텍스트 오버레이 — 이 씬 핵심

```
[화면 중앙, 가장 크게]
"우리가 기억합니다"
— SplitText, 글자 단위 stagger 0.08s
— font-size 3rem, Noto Serif KR
— 2.0s fade in

[중앙 하단]
"5·18민주화운동"
— font-size 1.2rem, letter-spacing 0.3em
— 0.8s delay 후 fade in

[B-구간 진입 시 추가, 묘지 모델 아래]
"국립5·18민주묘지"
— Noto Serif KR, 1.0rem, letter-spacing 0.2em
— B 구간 진행도 > 0.3에서 fade in

[하단 좌측, 작게]
"1980년 5월 18일 — 5월 27일, 광주"
— monospace, opacity 0.5
```

### 하단 출처 표기 (페이지 최하단 고정)

```html
<footer>
  지리 데이터: © OpenStreetMap contributors (ODbL).
  역사 자료: 5·18기념재단 · 5·18민주화운동기록관 · 민주화운동기념사업회.
  참고: 우리역사넷 · 한국민족문화대백과사전 · 한국어 위키백과.
  3D 모델: 한국학중앙연구원 광주문화예술인문스토리플랫폼 (CC BY-SA 4.0)
  — 옛_광주MBC · 전일빌딩245 · 5.18민주광장_분수대 · 국립5.18민주묘지.
  본 프로젝트도 동일 조건(CC BY-SA 4.0)으로 공개합니다.
</footer>
```

---

## 전체 셰이더 상태 요약

| 씬              | 흑백      | grain | vignette | 특수                              |
| --------------- | --------- | ----- | -------- | --------------------------------- |
| 00 배경         | 100%      | 0.10  | 0.30     | —                                 |
| 01 쿠데타       | 100%      | 0.12  | 0.40     | 빨간 파문                         |
| 02 시위         | 100%      | 0.08  | 0.30     | —                                 |
| 03 광주 확대    | 100%      | 0.10  | 0.35     | —                                 |
| 04 진압         | 100%      | 0.15  | 0.60     | 빨간 tint 0.08                    |
| 05 차량 시위    | 85%       | 0.10  | 0.30     | 노란빛 미세 허용                  |
| 06 광주MBC 방화 | 100%      | 0.15  | 0.55     | 빨간 tint 0.10, 화염 깜빡임       |
| 07 집단 발포    | 100%      | 0.18  | 0.70     | 빨간 tint 0.15, flash             |
| 08 전일빌딩     | 100%      | 0.18  | 0.70     | 빨간 tint 0.15, flash, 카메라 shake |
| 09 봉쇄         | 100%      | 0.12  | 0.50     | 빗금 오버레이                     |
| 10 시민궐기대회 | 95%       | 0.10  | 0.40     | 따뜻한 햇빛 (해방 광주)           |
| 11 도청         | 90%       | 0.14  | 0.60     | —                                 |
| 12 병원         | 50%       | 0.06  | 0.30     | —                                 |
| 13 기억/묘지    | 0% (컬러) | 0.03  | 0.10     | A:광주 빛 · B:묘지 따뜻한 light   |

---

## ScrollControls 씬 분기 구현 패턴

```jsx
// Experience.jsx
import { useScroll } from '@react-three/drei'
import { useFrame } from '@react-three/fiber'

const SCENE_RANGES = [
  [0.0000, 0.0714], // Scene 00 한국 지도
  [0.0714, 0.1429], // Scene 01 12·12
  [0.1429, 0.2143], // Scene 02 전국 시위
  [0.2143, 0.2857], // Scene 03 광주 확대
  [0.2857, 0.3571], // Scene 04 진압
  [0.3571, 0.4286], // Scene 05 차량 시위
  [0.4286, 0.5000], // Scene 06 광주MBC 방화 ★
  [0.5000, 0.5714], // Scene 07 집단 발포
  [0.5714, 0.6429], // Scene 08 전일빌딩 ★
  [0.6429, 0.7143], // Scene 09 봉쇄
  [0.7143, 0.7857], // Scene 10 시민궐기대회 ★
  [0.7857, 0.8571], // Scene 11 도청 진입
  [0.8571, 0.9286], // Scene 12 병원
  [0.9286, 1.0000], // Scene 13 기억/묘지 ★
]

export function useCurrentScene() {
  const scroll = useScroll()
  const [currentScene, setCurrentScene] = useState(0)

  useFrame(() => {
    const t = scroll.offset
    const idx = SCENE_RANGES.findIndex(([start, end]) => t >= start && t < end)
    if (idx !== -1 && idx !== currentScene) setCurrentScene(idx)
  })

  return currentScene
}

// 씬 내 로컬 진행도 (0~1)
export function useSceneProgress(sceneIndex) {
  const scroll = useScroll()
  const [start, end] = SCENE_RANGES[sceneIndex]
  return Math.max(0, Math.min(1, (scroll.offset - start) / (end - start)))
}
```

---

## 구현 순서 (MVP 기준)

1. Scene 00 — 한국 지도 메시 + 기본 카메라
2. Scene 03 — 광주 확대 전환 (카메라 줌 검증)
3. Scene 09 — 봉쇄 빗금 (셰이더 오버레이 검증)
4. Scene 04 — 최루탄 파티클 시스템
5. Scene 05 — 차량 시위 (geometry 다수 배치)
6. Scene 07 — 발포 (flash uniform 검증)
7. Scene 01, 02 — 마커 시스템
8. Scene 11, 12 — 도청·병원
9. **GLB 통합 4개 씬**: Scene 06 광주MBC → 08 전일빌딩 → 10 시민궐기대회 → 13 묘지
10. Scene 13 — 마무리 (B-구간 묘지 트랜지션 포함)

---

## 한국 지도 GeoJSON 소스

한반도 SVG/GeoJSON (무료, 공개 도메인):

```
https://raw.githubusercontent.com/southkorea/southkorea-maps/master/kostat/2012/json/skorea_provinces_geo_simple.json
```

Three.js에서 GeoJSON → ShapeGeometry 변환:

```javascript
import { Feature } from 'geojson'

function geoJsonToShape(feature) {
  const shape = new THREE.Shape()
  const coords = feature.geometry.coordinates[0]
  coords.forEach(([lng, lat], i) => {
    const x = (lng - 127.5) * 2.5
    const y = (lat - 36.5) * 3.0
    i === 0 ? shape.moveTo(x, y) : shape.lineTo(x, y)
  })
  return shape
}
```
